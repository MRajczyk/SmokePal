import React from "react";

const WeatherForecastRecord = ({
  icon,
  day,
  temperature,
}: {
  icon: string;
  day: string;
  temperature: string;
}) => {
  return <div>WeatherForecastRecord</div>;
};

export default WeatherForecastRecord;
